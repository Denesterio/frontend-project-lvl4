// import React from 'react';
// import BaseModal from './BaseModal.jsx';
// import { addToForm, createForm, getWithSubmitHander } from '../utils/formBuilder.jsx';

// const ModalWrapper = ({ event, children }) => {
//   const form = createForm({ name: `${event}-form` })
//     |> (addToForm({ type: 'text', name: 'name', placeholder: 'Введите название канала' }))
//     |> (getWithSubmitHander((values) => console.dir(values)));

//       <BaseModal modalTitle="Добавить канал">
//         {{
//           body: children.form,
//           confirmButton: children.button,
//         }}
//       </BaseModal>;
// };
